import { resolve as resolvePath } from 'path';

export const rootDir = resolvePath(__dirname, '../');
export const pluginsDir = resolvePath(__dirname, './plugins/');
export const eventsDir = resolvePath(__dirname, './events/');
